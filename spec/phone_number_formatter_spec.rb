require_relative './spec_helper.rb'
require_relative '../src/phone_number_formatter.rb'

describe 'format' do

  it 'receives a correctly formatted number and returns it' do

  end

  it 'receives an eleven digit long correct number and returns the correct format' do

  end

  it 'receives an eleven digit long number with spaces and returns the correct format' do

  end

  it 'receives an incorrect number with less than eleven digits and raises an error' do

  end

  it 'receives an incorrect number with more than eleven digits and raises an error' do

  end

  it 'receives an incorrect string with non-numeric characters and raises an error' do
    
  end
end